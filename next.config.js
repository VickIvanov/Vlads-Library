/** @type {import('next').NextConfig} */
const nextConfig = {
  // Убеждаемся, что API routes работают
  experimental: {
    // Отключаем экспериментальные функции, которые могут мешать
  },
};

export default nextConfig;

