# Быстрая настройка Google OAuth

## ⚠️ Ошибка: "Google OAuth не настроен"

Если вы видите эту ошибку, нужно настроить Google OAuth.

## 📝 Шаг 1: Создайте файл .env

Создайте файл `.env` в корне проекта (рядом с `package.json`) со следующим содержимым:

```env
GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret-here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

## 🔑 Шаг 2: Получите Google OAuth credentials

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Перейдите в **APIs & Services** → **Credentials**
4. Нажмите **Create Credentials** → **OAuth client ID**
5. Если это первый раз, настройте OAuth consent screen:
   - Выберите **External** (для тестирования)
   - Заполните обязательные поля:
     - App name: "Cosmic Library"
     - User support email: ваш email
     - Developer contact email: ваш email
   - Сохраните и продолжите

## 🌐 Шаг 3: Создайте OAuth Client ID

1. Выберите тип приложения: **Web application**
2. Укажите **Authorized redirect URIs**:
   
   **Для localhost (Next.js):**
   ```
   http://localhost:3000/api/auth/google/callback
   ```
   
   **Для production (ваш домен):**
   ```
   https://your-domain.com/api/auth/google/callback
   ```
   
   **Для Vercel:**
   ```
   https://your-app.vercel.app/api/auth/google/callback
   ```

3. Сохраните и скопируйте:
   - **Client ID** (например: `123456789-abc.apps.googleusercontent.com`)
   - **Client Secret** (например: `GOCSPX-abc123def456`)

## ✏️ Шаг 4: Добавьте credentials в .env

Откройте файл `.env` и замените значения:

```env
GOOGLE_CLIENT_ID=123456789-abc.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-abc123def456
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

## 🔄 Шаг 5: Перезапустите сервер

После добавления переменных окружения перезапустите сервер:

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова
npm run dev
```

## ✅ Проверка

1. Откройте сайт
2. Нажмите "Войти"
3. Нажмите "Войти через Google"
4. Выберите аккаунт Google
5. После авторизации вы будете перенаправлены обратно

## ⚠️ Важно

- **Redirect URI должен точно совпадать** с тем, что указан в Google Cloud Console
- Для Next.js обычно используется порт **3000**, а не 5000
- Файл `.env` уже добавлен в `.gitignore` и не будет закоммичен
- **Никогда не делитесь** `GOOGLE_CLIENT_SECRET` публично

## 🐛 Если не работает

1. Проверьте, что файл `.env` находится в корне проекта
2. Убедитесь, что переменные названы правильно (без пробелов)
3. Проверьте, что Redirect URI в Google Console точно совпадает с вашим URL
4. Перезапустите сервер после изменения `.env`
5. Проверьте консоль браузера и терминал на наличие ошибок

