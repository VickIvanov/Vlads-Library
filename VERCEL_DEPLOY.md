# 🚀 Деплой на Vercel

## ✅ Репозиторий на GitHub готов!
🔗 https://github.com/VickIvanov/Vlads-Library

## 📋 Шаги для деплоя на Vercel:

1. **Откройте Vercel** (браузер уже открыт)
   - Или перейдите: https://vercel.com/new

2. **Войдите через GitHub** (если не вошли)

3. **Импортируйте репозиторий:**
   - Нажмите "Add New Project"
   - Выберите репозиторий: **Vlads-Library**
   - Vercel автоматически определит Next.js

4. **Настройте переменные окружения:**
   После деплоя в настройках проекта добавьте (или обновите) переменные:
   ```
   LIBRARY_USERS=admin:admin123,user:user123
   ADMIN_USERNAME=admin
   ADMIN_PASSWORD=admin123
   NODE_ENV=production
   BOOKS_DIR_NAME=123          # имя папки, куда складываются .txt файлы
   ```
   > 📂 Папка с именем `123` будет автоматически создаваться как локально, так и на Vercel (внутри `/tmp/cosmic-library/123`).  
   > Если нужно другое имя, просто поменяйте значение `BOOKS_DIR_NAME`. Для полностью кастомного пути можно использовать `BOOKS_DIR` или `RUNTIME_DATA_DIR`.

5. **Нажмите "Deploy"**

6. **После деплоя:**
   - Перейдите в Settings → Environment Variables
   - Добавьте переменные (см. выше)
   - Перейдите в Deployments
   - Нажмите на последний деплой → "Redeploy"

## ✨ Готово!

Ваше приложение будет доступно по адресу:
`https://vlads-library.vercel.app` (или похожему)

