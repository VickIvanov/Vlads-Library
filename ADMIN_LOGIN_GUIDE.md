# Как войти администратором

## 🔑 Данные для входа по умолчанию

**Логин:** `admin`  
**Пароль:** `admin123`

## 📝 Шаги для входа

1. Откройте главную страницу сайта
2. Нажмите кнопку **"🔐 Войти"**
3. Введите:
   - **Имя пользователя:** `admin`
   - **Пароль:** `admin123`
4. Нажмите **"Войти"**

## ⚙️ Изменение данных администратора

Если вы хотите изменить логин и пароль администратора:

1. Создайте или откройте файл `.env` в корне проекта
2. Добавьте или измените следующие строки:

```env
ADMIN_USERNAME=ваш_логин
ADMIN_PASSWORD=ваш_пароль
```

3. Перезапустите сервер

## ✅ Что может делать администратор

После входа как администратор вы получите доступ к:

- ➕ **Добавить книгу** - кнопка для добавления новых книг в библиотеку
- 👥 **Пользователи** - просмотр списка всех зарегистрированных пользователей

## 🔒 Безопасность

- **Никогда не делитесь** паролем администратора
- Измените пароль по умолчанию в production окружении
- Файл `.env` уже добавлен в `.gitignore` и не будет закоммичен в Git

## ⚠️ Важно

- Администратор определяется через переменные окружения `ADMIN_USERNAME` и `ADMIN_PASSWORD`
- Если эти переменные не заданы, используются значения по умолчанию: `admin` / `admin123`
- Администратор НЕ может быть создан через обычную регистрацию - только через настройку `.env`

