# Портативная настройка проекта

Проект теперь полностью портативен и работает в любом окружении без изменений кода.

## 📁 Структура файлов

Все файлы данных создаются автоматически в корне проекта:

- `database.json` - база данных (пользователи, настройки, закладки)
- `books.json` - список книг
- `books/` - папка с файлами книг (.txt)

## 🔧 Переменные окружения (опционально)

Если нужно изменить расположение файлов, можно использовать переменные окружения:

### Для локальной разработки

Создайте файл `.env` в корне проекта:

```env
# Путь к файлу базы данных (по умолчанию: database.json)
DB_FILE=database.json

# Путь к файлу со списком книг (по умолчанию: books.json)
BOOKS_FILE=books.json

# Путь к папке с файлами книг (по умолчанию: books)
BOOKS_DIR=books
```

### Для продакшена

Установите переменные окружения на вашем сервере:

```bash
export DB_FILE=/path/to/database.json
export BOOKS_FILE=/path/to/books.json
export BOOKS_DIR=/path/to/books
```

## ✅ Что изменилось

1. **Убраны зависимости от Vercel** - код работает везде одинаково
2. **Универсальные пути** - используется `process.cwd()` для определения корня проекта
3. **Опциональные переопределения** - можно настроить через переменные окружения
4. **Автоматическое создание** - все файлы и папки создаются автоматически

## 🚀 Развертывание

Просто скопируйте проект в любую папку и запустите:

```bash
npm install
npm run dev
```

Все файлы данных будут созданы автоматически в корне проекта.

## 📝 Примечания

- Если указать абсолютный путь в переменных окружения (начинается с `/`), он будет использован как есть
- Если указать относительный путь, он будет добавлен к корню проекта
- Если переменные окружения не указаны, используются значения по умолчанию

