# 🚀 Инструкция по деплою на Vercel

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Название репозитория: **"библиотека-Влада"** (или **"biblioteka-vlada"**)
4. Выберите **Public** или **Private** (по желанию)
5. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
6. Нажмите **"Create repository"**

## Шаг 2: Загрузка кода на GitHub

### Если Git установлен:

```bash
# Инициализация репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Библиотека Влада"

# Добавление remote репозитория (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/библиотека-Влада.git

# Отправка кода
git branch -M main
git push -u origin main
```

### Если Git не установлен:

1. Скачайте и установите [Git for Windows](https://git-scm.com/download/win)
2. После установки выполните команды выше

### Альтернативный способ (через GitHub Desktop):

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Откройте GitHub Desktop
3. File → Add Local Repository → выберите папку проекта
4. Введите commit message и нажмите "Commit to main"
5. Publish repository → введите название "библиотека-Влада"

## Шаг 3: Деплой на Vercel

1. Перейдите на [Vercel](https://vercel.com) и войдите через GitHub
2. Нажмите **"Add New Project"**
3. Выберите репозиторий **"библиотека-Влада"**
4. Vercel автоматически определит Next.js
5. Нажмите **"Deploy"**

## Шаг 4: Настройка переменных окружения на Vercel

После деплоя:

1. Перейдите в настройки проекта на Vercel
2. Откройте раздел **"Environment Variables"**
3. Добавьте следующие переменные:

```
LIBRARY_USERS=admin:admin123,user:user123
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
NODE_ENV=production
```

4. Нажмите **"Save"**
5. Перейдите в раздел **"Deployments"**
6. Нажмите на последний деплой → **"Redeploy"**

## Шаг 5: Готово! 🎉

Ваше приложение будет доступно по адресу:
`https://ваш-проект.vercel.app`

## ⚠️ Важные замечания

1. **Файлы данных**: `database.json` и `books.json` не включены в репозиторий (в .gitignore). 
   - На Vercel они будут созданы автоматически при первом использовании
   - Или вы можете создать их вручную через Vercel CLI

2. **Загрузка книг**: После деплоя вам нужно будет добавить книги через интерфейс администратора

3. **Пароли**: Обязательно измените пароли администратора в production!

## 🔧 Обновление кода

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически задеплоит новую версию!

