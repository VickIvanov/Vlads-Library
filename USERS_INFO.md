# Где хранятся логины и пароли пользователей

## 📍 Места хранения

### 1. Администратор (`.env` файл)
**Файл:** `.env` в корне проекта

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

**По умолчанию:**
- Логин: `admin`
- Пароль: `admin123`

**Использование:** Вход в админ-панель для добавления/удаления книг

---

### 2. Обычные пользователи (`users.json`)
**Файл:** `users.json` (на localhost) или `/tmp/users.json` (на Vercel)

**Создаётся автоматически** при первой регистрации пользователя через форму регистрации.

**Формат:**
```json
[
  {
    "username": "user1",
    "password": "password123",
    "created_at": "1762371788.3495793",
    "role": "user"
  },
  {
    "username": "user2",
    "password": "password456",
    "created_at": "1762371800.1234567",
    "role": "user"
  }
]
```

**Как добавить:** Через форму регистрации на главной странице

---

### 3. Пользователи из `.env` (`LIBRARY_USERS`)
**Файл:** `.env` в корне проекта

```env
LIBRARY_USERS=user1:pass1,user2:pass2,user3:pass3
```

**Формат:** `username:password,username2:password2`

**Приоритет:** Эти пользователи проверяются **первыми** при входе

---

## 🔍 Порядок проверки при входе

1. **Пользователи из `.env`** (`LIBRARY_USERS`) - проверяются первыми
2. **Пользователи из `users.json`** - проверяются вторыми
3. **Администратор** из `.env` (`ADMIN_USERNAME` / `ADMIN_PASSWORD`) - для админ-панели

---

## ⚠️ Важно

- **Пароли хранятся в открытом виде** (без шифрования)
- Файл `.env` не должен попадать в git (уже в `.gitignore`)
- Файл `users.json` можно редактировать вручную или через форму регистрации
- На Vercel используйте **Environment Variables** в настройках проекта

---

## 📝 Как добавить пользователя

### Вариант 1: Через форму регистрации
1. Откройте главную страницу
2. Нажмите кнопку меню (☰)
3. Выберите "Регистрация"
4. Введите логин и пароль

### Вариант 2: Через `.env` файл
Добавьте в `.env`:
```env
LIBRARY_USERS=user1:pass1,user2:pass2
```

### Вариант 3: Вручную в `users.json`
Добавьте объект в массив (после создания файла через регистрацию)

---

## 🔐 Безопасность

⚠️ **Внимание:** Пароли хранятся в открытом виде. Для продакшена рекомендуется:
- Использовать хеширование паролей (bcrypt, argon2)
- Использовать HTTPS
- Ограничить доступ к файлам `.env` и `users.json`



## 📍 Места хранения

### 1. Администратор (`.env` файл)
**Файл:** `.env` в корне проекта

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

**По умолчанию:**
- Логин: `admin`
- Пароль: `admin123`

**Использование:** Вход в админ-панель для добавления/удаления книг

---

### 2. Обычные пользователи (`users.json`)
**Файл:** `users.json` (на localhost) или `/tmp/users.json` (на Vercel)

**Создаётся автоматически** при первой регистрации пользователя через форму регистрации.

**Формат:**
```json
[
  {
    "username": "user1",
    "password": "password123",
    "created_at": "1762371788.3495793",
    "role": "user"
  },
  {
    "username": "user2",
    "password": "password456",
    "created_at": "1762371800.1234567",
    "role": "user"
  }
]
```

**Как добавить:** Через форму регистрации на главной странице

---

### 3. Пользователи из `.env` (`LIBRARY_USERS`)
**Файл:** `.env` в корне проекта

```env
LIBRARY_USERS=user1:pass1,user2:pass2,user3:pass3
```

**Формат:** `username:password,username2:password2`

**Приоритет:** Эти пользователи проверяются **первыми** при входе

---

## 🔍 Порядок проверки при входе

1. **Пользователи из `.env`** (`LIBRARY_USERS`) - проверяются первыми
2. **Пользователи из `users.json`** - проверяются вторыми
3. **Администратор** из `.env` (`ADMIN_USERNAME` / `ADMIN_PASSWORD`) - для админ-панели

---

## ⚠️ Важно

- **Пароли хранятся в открытом виде** (без шифрования)
- Файл `.env` не должен попадать в git (уже в `.gitignore`)
- Файл `users.json` можно редактировать вручную или через форму регистрации
- На Vercel используйте **Environment Variables** в настройках проекта

---

## 📝 Как добавить пользователя

### Вариант 1: Через форму регистрации
1. Откройте главную страницу
2. Нажмите кнопку меню (☰)
3. Выберите "Регистрация"
4. Введите логин и пароль

### Вариант 2: Через `.env` файл
Добавьте в `.env`:
```env
LIBRARY_USERS=user1:pass1,user2:pass2
```

### Вариант 3: Вручную в `users.json`
Добавьте объект в массив (после создания файла через регистрацию)

---

## 🔐 Безопасность

⚠️ **Внимание:** Пароли хранятся в открытом виде. Для продакшена рекомендуется:
- Использовать хеширование паролей (bcrypt, argon2)
- Использовать HTTPS
- Ограничить доступ к файлам `.env` и `users.json`






