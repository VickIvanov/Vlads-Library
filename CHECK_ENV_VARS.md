# 🔍 Проверка переменных окружения для Neon

## Проблема: База данных не находится

Если вы видите ошибку "База данных не настроена", это означает, что переменные окружения не доступны в runtime.

## ✅ Решение: Добавьте переменные вручную

Vercel может не применять переменные окружения из Storage автоматически. Нужно добавить их вручную:

### Шаг 1: Скопируйте значение DATABASE_URL

1. Откройте проект на Vercel: https://vercel.com/dashboard
2. Перейдите в **Storage** → найдите вашу Neon базу данных
3. Нажмите на базу данных
4. Найдите переменную **DATABASE_URL** (она будет скрыта звездочками)
5. Нажмите на иконку копирования рядом с **DATABASE_URL**
6. Скопируйте значение

### Шаг 2: Добавьте переменную в проект

1. В проекте Vercel перейдите в **Settings** → **Environment Variables**
2. Нажмите **Create new**
3. Заполните:
   - **Key**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение из Neon
   - **Environment**: выберите **Production**, **Preview**, и **Development** (или только Production)
4. Нажмите **Save**

### Шаг 3: Перезапустите деплой

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **⋮** → **Redeploy**
4. Дождитесь завершения

### Шаг 4: Проверьте работу

После перезапуска откройте сайт и попробуйте добавить книгу. Если всё работает - база данных подключена! ✅

---

## 🔍 Альтернатива: Используйте переменные с префиксом

Если Vercel автоматически добавил переменные с префиксом `library_*`, код уже поддерживает их. Но для надежности лучше добавить `DATABASE_URL` без префикса.

---

## ❓ Проблемы?

### Переменная не копируется:
- Попробуйте открыть Neon Console напрямую
- Или используйте переменную `library_DATABASE_URL` (если она есть)

### Переменная не применяется:
- Убедитесь, что выбрали правильные Environment (Production, Preview, Development)
- Перезапустите деплой после добавления переменной

### Всё ещё не работает:
- Проверьте логи Vercel (Deployments → Functions → Logs)
- Найдите логи с `[DB]` или `[DB-CONNECTION]`
- Пришлите логи для диагностики

