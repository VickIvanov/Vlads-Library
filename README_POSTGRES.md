# 🗄️ PostgreSQL интеграция - Быстрый старт

## ✅ Что сделано

1. ✅ Установлены зависимости: `@vercel/postgres` и `pg`
2. ✅ Создана система работы с PostgreSQL (`lib/db-pg.js`)
3. ✅ Добавлена система логирования в БД (`lib/logger.js`)
4. ✅ Обновлены все API роуты для работы с PostgreSQL
5. ✅ Добавлена автоматическая инициализация БД при первом запросе
6. ✅ Созданы скрипты миграции данных

## 🚀 Что нужно сделать

### 1. Создать PostgreSQL базу данных

**На Vercel:**
- Откройте проект в Vercel Dashboard
- Перейдите в **Storage** → **Create Database** → **Postgres**
- Следуйте инструкциям
- Vercel автоматически добавит переменные окружения

**Локально (для тестирования):**
- Установите PostgreSQL или используйте Docker
- Создайте базу данных
- Добавьте переменные в `.env.local`:
  ```
  POSTGRES_URL=postgresql://user:password@localhost:5432/database
  POSTGRES_PRISMA_URL=postgresql://user:password@localhost:5432/database
  POSTGRES_URL_NON_POOLING=postgresql://user:password@localhost:5432/database
  POSTGRES_USER=user
  POSTGRES_HOST=localhost
  POSTGRES_PASSWORD=password
  POSTGRES_DATABASE=database
  ```

### 2. Инициализировать БД

База данных автоматически инициализируется при первом запросе к API.

Или вручную:
```bash
npm run init-db
```

### 3. Мигрировать существующие данные (опционально)

Если у вас есть данные в `database.json` и `books.json`:
```bash
npm run migrate
```

## 📊 Структура БД

- **users** - Пользователи
- **books** - Книги  
- **bookmarks** - Закладки
- **settings** - Настройки
- **logs** - Логи всех действий

## 🔍 Логирование

Все действия автоматически логируются:
- ✅ Регистрация пользователей
- ✅ Вход в систему
- ✅ Добавление/удаление книг
- ✅ Ошибки и предупреждения

Просмотр логов: `/api/logs` (только для администраторов)

## ⚠️ Важно

- **Кодировка UTF-8** настроена автоматически - кириллица поддерживается
- Данные **не удаляются** при перезапуске (в отличие от `/tmp`)
- Все действия **логируются** в БД
- Старые JSON файлы можно удалить после миграции

## 🐛 Решение проблем

**Ошибка подключения к БД:**
- Проверьте переменные окружения
- Убедитесь, что БД создана и доступна

**Кириллица отображается как знаки вопроса:**
- Кодировка UTF-8 установлена автоматически
- Проверьте настройки БД (должна быть UTF-8)

**Пользователи/книги пропадают:**
- Теперь данные в PostgreSQL - они не должны пропадать
- Проверьте подключение к БД

